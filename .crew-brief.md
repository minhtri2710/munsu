# Task brief: munsu-rank-rename

## Setup
You are in a disposable git worktree of munsu, at a detached HEAD on a clean default branch.

**Verify isolation before anything else.** Run `pwd -P` and `git rev-parse --show-toplevel`; both must resolve to the disposable task worktree you were launched in, such as a treehouse pool path or an Orca-managed worktree, not the primary checkout munsu operates from.
The path check is authoritative: `git rev-parse --git-dir` and `git rev-parse --git-common-dir` can help inspect the repo, but they do not prove you are outside the primary checkout.
If the top-level path is the primary checkout or not the worktree you were launched in, STOP - do not branch or commit here - append `blocked: launched in primary checkout, not an isolated worktree` to the status file and stop.

1. First action: create your branch: `git checkout -b fm/munsu-rank-rename`

Delivery mode: direct-PR
## Delivery
	Commit the completed change, push the feature branch, and open a PR directly against the default branch.
	Never run no-mistakes for this task. Never merge the PR.



## Task
Full breaking rename of munsu rank vocabulary to battlefield hierarchy:

- **General** = munsu fleet orchestrator / primary home / fleet CLI surface
- **Captain** = persistent domain supervisor (was captain)
- **Soldier** = task worker (was soldier)

Hierarchy: General → Captain → Soldier.

Base from main at cf8562f (spawn authority/topology). Do not regress fish-safe launch or workspace topology.

### Scope (must)
1. CLI verbs/help: `captain` → `captain` (clean break preferred; temporary alias only if required).
2. Runtime identity: `MUNSU_ROLE=captain|soldier` → `captain|soldier` (and general if present).
3. Meta kinds: `kind=captain` → `kind=captain` (keep soldier kinds consistent).
4. Labels/workspaces: replace `mu-captain-*` / `captain-*` with one rank-consistent scheme; document it.
5. Packages/symbols/messages including `internal/captain` → captain-rank naming.
6. Docs + embedded skills (`munsu-ops`, captain-provisioning → captain-provisioning naming).
7. Tests + one live lifecycle probe after rename.
8. PR body migration notes: old homes/meta/env must migrate or fail closed with a clear repair command.

### Non-goals
- Watcher/guard fixes
- Husk cleanup
- Live multi-harness validation

### Done when
- `go test ./...` green
- `munsu captain ...` works; old `captain` removed or temporary alias documented
- Spawn authority still fail-closed under new role names
- PR opened against main with migration notes
- append `done: PR {url}` (checks may still be running; Captain will record via munsu pr-check)

## Rules
1. Never push to the default branch. Never merge a PR.
2. Stay inside this worktree; modify nothing outside it.
3. Use gh-axi for GitHub operations and chrome-devtools-axi for browser operations.
4. Report status by appending one line:
   `munsu task status munsu-rank-rename {state} "message"`
   Each append signals munsu, so report sparingly: only phase changes a supervisor
   would act on and the needs-decision/blocked/paused/done/failed states.
5. If you hit the same obstacle twice, append `blocked: {why}` and stop; munsu will help.
6. If a decision belongs to a human, append `needs-decision: {summary of options}` and stop.
7. To close an open wake key, append `resolved [key=<slug>]: {summary}`. Repeating the same resolved key is safe.
8. Never stop, restart, or update the shared `no-mistakes` daemon - it is one instance serving every lane/home, so restarting it kills other lanes' in-flight pipeline runs. On ANY no-mistakes daemon error, append `blocked: {the daemon error}` and stop; only munsu manages the daemon.

## Project memory
If `AGENTS.md` or `CLAUDE.md` already exists, or if this task produced durable project-intrinsic knowledge, run `munsu ensure-agents-md .`.
Record only project knowledge useful to almost every future session.

## Definition of done
The task is complete only when committed on your branch.
When delivery is complete, append `done: {summary}` to the status file and stop.
Before that, close every open keyed decision with `resolved [key=<slug>]: {summary}`.
