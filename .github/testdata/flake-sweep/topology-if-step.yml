jobs:
  invariants:
    name: Repo invariants
    permissions:
      contents: read
      actions: read
    steps:
      - if: ${{ false }}
        name: gate
        run: .github/scripts/flake-sweep.sh applied
