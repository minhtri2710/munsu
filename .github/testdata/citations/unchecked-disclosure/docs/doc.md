The subcommands are `get/set`, the dependency is `github.com/spf13/cobra`, and
the runtime file is `state/.afk-digest`. None is a claim about a file here.

Unsupported selectors are `newCaptainRecoverTransaction().Recover()` and `newCaptainRecoverTransaction(context.Background()).Recover`.
