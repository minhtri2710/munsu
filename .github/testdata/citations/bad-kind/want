::error::.github/testdata/citations/bad-kind/waiver:2: docs/doc.md: unknown kind file -- the tool emits path or symbol
::error::citations that do not resolve and are not in .github/testdata/citations/bad-kind/waiver:
  docs/doc.md: symbol home.ReadMailboxEnvelope
    docs/doc.md:1:The document cites `home.WriteMailboxEnvelope` and `home.ReadMailboxEnvelope` in
  A path citation must name a file that exists; a symbol citation must name a func,
  method, type, const, var or struct field this tree declares. Correct the citation,
  or add a line to that file with a reason -- see ADR-0017 4 for what a reason has to say.
::error::.github/testdata/citations/bad-kind/waiver entries that now resolve, or that no document makes any more:
  docs/doc.md: file home.ReadMailboxEnvelope
  Each was either corrected or removed -- both are good news. Drop the line.
exit 1
