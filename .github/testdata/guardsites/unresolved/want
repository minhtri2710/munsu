::error::guardsites could not scan the tree, so it cannot derive the guard set: type info resolved no spans for any of the 1 files the walk found, so the resolver's output is unusable for this tree and every type-dependent guard would silently fall back to the name heuristic
exit status 1
exit 1
