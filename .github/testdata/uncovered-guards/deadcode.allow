# A two-entry stand-in for .github/deadcode.allow, so the summary line in every
# `want` below stays put while the real register changes.
internal/fake/a.go	Dead.One	fixture waiver one
internal/fake/b.go	Dead.Two	fixture waiver two
