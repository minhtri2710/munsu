::error::.github/testdata/uncovered-guards/bad-nth/baseline:1: internal/fake/guard.go: Refuse: nth must be a number, got "x"
::warning file=internal/fake/gated_linux.go::no lane compiles this file on this platform, so its refusal branches are outside the lane in both directions
::error::refusal branches no test enters, and not in .github/testdata/uncovered-guards/bad-nth/baseline:
  internal/fake/guard.go:28.22: Refuse (#1): if len(failures) > 0
  Nothing has ever taken these branches, so nothing would notice if the condition were
  inverted or the branch deleted. Write a test that builds the state the guard refuses --
  5 to 15 lines for the ones already burnt down. If the branch genuinely cannot be reached
  from a test, add a line to that file with a reason.
::error::.github/testdata/uncovered-guards/bad-nth/baseline entries that match no refusal branch in the tree:
  internal/fake/guard.go: Refuse (#x): if len(failures) > 0
  Each was deleted or rewritten -- both are fine. Drop the line.
exit 1
