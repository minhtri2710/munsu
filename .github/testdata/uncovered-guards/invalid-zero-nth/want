::error::.github/testdata/uncovered-guards/invalid-zero-nth/baseline:1: internal/fake/guard.go: Refuse: nth must be a positive number, got "0"
::error::refusal branches no test enters, and not in .github/testdata/uncovered-guards/invalid-zero-nth/baseline:
  internal/fake/guard.go:20.25-22.4: Refuse (#1): if identity != Worktree
  internal/fake/guard.go:28.22-30.4: Refuse (#1): if len(failures) > 0
  Nothing has ever taken these branches, so nothing would notice if the condition were
  inverted or the branch deleted. Write a test that builds the state the guard refuses --
  5 to 15 lines for the ones already burnt down. If the branch genuinely cannot be reached
  from a test, add a line to that file with a reason.
::error::.github/testdata/uncovered-guards/invalid-zero-nth/baseline entries that match no refusal branch in the tree:
  internal/fake/guard.go: Refuse (#0): if len(failures) > 0
  Each was deleted or rewritten -- both are fine. Drop the line.
exit 1
