package beo109probe

// BEO-109 verification probe. Deliberately does not compile so every required
// lane on this PR is red and auto-merge can never complete. Delete with the
// branch.
func broken() int { return "not an int" }
